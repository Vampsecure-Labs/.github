<!-- 
  English below / Español más abajo
-->

## Description / Descripción

<!-- EN: What does this PR do? Why is it needed?
     ES: ¿Qué hace este PR? ¿Por qué es necesario? -->

## Tool(s) affected / Herramienta(s) afectada(s)

<!-- List the VSL tools changed / Lista las herramientas VSL modificadas -->
-

## Type of change / Tipo de cambio

- [ ] Bug fix / Corrección de error
- [ ] New feature / Nueva funcionalidad
- [ ] Refactor (no behavior change / sin cambio de comportamiento)
- [ ] Documentation / Documentación
- [ ] New tool / Nueva herramienta

## Checklist / Lista de verificación

- [ ] Tested locally against a real or mock target / Probado localmente contra un objetivo real o simulado
- [ ] JSON output follows the VSL findings schema (`id`, `severity`, `title`, `description`, `evidence`, `remediation`) / La salida JSON sigue el schema VSL de findings
- [ ] No hardcoded credentials, tokens, or real target data / Sin credenciales, tokens ni datos de objetivo reales en el código
- [ ] No `.env`, `*.key`, `*.pem`, or `scope.txt` files staged / Sin ficheros `.env`, `*.key`, `*.pem` ni `scope.txt` en el stage
- [ ] `--help` output updated if new flags were added / Salida de `--help` actualizada si se añadieron nuevos flags
- [ ] Compatible with `vamp-orchestrator` and `vamp-penreport` if the tool is in the catalog / Compatible con `vamp-orchestrator` y `vamp-penreport` si la herramienta está en el catálogo

## Test output / Salida de prueba

<!-- Paste a trimmed excerpt of the tool's console or JSON output after your change
     Pega un fragmento de la salida de consola o JSON de la herramienta tras el cambio -->

```
```

## Notes / Notas

<!-- EN: Anything reviewers should pay attention to, known limitations, or follow-up work.
     ES: Aspectos a los que los revisores deben prestar atención, limitaciones conocidas o trabajo pendiente. -->
